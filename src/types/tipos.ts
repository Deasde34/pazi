// tipos.ts

// Define el tipo de dato para un registro en la base de datos
export type DatosType = {
  nombre: string;
  apellido: string;
  fechaNacimiento: string; // Ajusta el tipo según el formato real de fecha
  profesional: string;
  hora: string;
  fecha: string; // Ajusta el tipo según el formato real de fecha
  razonVisita: string;
};